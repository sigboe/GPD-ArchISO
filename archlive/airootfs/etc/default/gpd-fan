# Change defaults of one or none/any of the vars!
# activate custom values:
# sudo mv /etc/default/gpd-fan.example /etc/default/gpd-fan

# Time between temperature checks (in seconds)
TIME=10

# Maximum temperature before turbo boost is disabled
TURBO=85

# Temperature to set fans to minimum speed
MIN=55

# Temperature to set fans to medium speed
MED=65

# Temperature to set fans to maximum speed
MAX=80

# Set fan speed to minimum and sleep for 3 seconds on start
# blow nozzles
FEEDBACK=true

# debug, info, warn, error, fatal, unknown
LOGLEVEL=info
